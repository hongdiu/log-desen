# AGENTS.md — 任务执行总纲



状态机：IDLE → PLAN(方案) → WAIT(批准) → CODE(改+测+脚本) → BUILD(构建) → FIX(自动修≤5轮) → DONE(交付)。不可逆、不可跳。

全程中文回答。

## 1. 铁律（违禁即错）
- 无方案无批准 = 禁改代码（方案必须含：目标/范围/步骤/风险）。
- 禁改无关代码。
- 编译/测试不通过 = 任务未完成。完成时执行 git add。

## 2. 自动修复边界（关键决策点）
- 免批自动修：编译报错、类型/API错误、依赖缺失、测试语法错。
- 必须报备修：影响业务逻辑、UI行为、数据存储的功能性变更。

## 3. 硬性产出（缺一不可）
- 大任务拆子任务（由简至繁），每步独立编译通过再下一步。
- 生成测试用例 → 输出 build_test.sh 跑全测 → 捕获失败自动修≤5轮（功能变更需报备）→ 最终交付通过代码+测试+日志。



## 4. Subagent硬性要求

所有任务必须使用 delegate_task 派发给子Agent执行，强制参数：

```
delegate_task(
  task="描述任务，代码编写/编译/调试",
  runTimeoutSeconds=300,
  background=true
)
```

规则：
1. 子Agent最多运行5分钟，超时直接销毁，新建子Agent重试；
2. 远程大模型接口25秒无响应直接断开；
3. 30秒无流式输出判定模型卡死，终止本次调用。
3. 构建编译必须前台跑,要看到它的编译输出日志;



**响应前自检（必问）**：
当前状态？方案获批否？编译过了？轮次超限否？
